{
  "name": "ecommerce-mern-starter",
  "private": true,
  "workspaces": [
    "admin",
    "user",
    "backend"
  ],
  "scripts": {
    "dev": "echo \"Run admin, user, and backend from their own folders\"",
    "dev:admin": "npm run dev --workspace admin",
    "dev:user": "npm run dev --workspace user",
    "dev:backend": "npm run dev --workspace backend"
  }
}
