{
  "name": "ecommerce-mern-starter",
  "private": true,
  "workspaces": [
    "admin",
    "user",
    "backend"
  ],
  "scripts": {
    "dev": "echo \"Use dev:backend, dev:user, and dev:admin from the root\"",
    "start": "echo \"Start the individual apps from admin, user, or backend\"",
    "dev:admin": "npm run dev --workspace admin",
    "dev:user": "npm run dev --workspace user",
    "dev:backend": "npm run dev --workspace backend"
  }
}
